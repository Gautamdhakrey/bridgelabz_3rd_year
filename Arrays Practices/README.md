# 📗 Arrays Practices

Assignments focused on practicing Java Arrays, Array operations, and Multi-dimensional Arrays.

---

### Level 1 – Topics Covered (10 Questions)

1. Age of 10 students → check voting eligibility  
2. Input 5 numbers → classify as positive/negative/zero + even/odd check + compare first & last elements  
3. Multiplication table of a number (1–10)  
4. Store values in array (max 10 or until user enters ≤0) and find sum  
5. Multiplication table of a number from 6 to 9  
6. Mean height of 11 football players  
7. Separate odd and even numbers into arrays (1..n)  
8. Find factors of a number and store them in an array (dynamic resizing)  
9. Copy elements of a 2D array into a 1D array  
10. FizzBuzz program using arrays  

---

### Level 2 – Topics Covered (10 Questions)

1. Sum of digits of a number using array  
2. 2D array (matrix) input → display + sum of all elements  
3. Employee bonus calculation for 10 employees (old salary, new salary, total bonus)  
4. Youngest (age) and Tallest (height) among 3 friends  
5. Store digits of a number in array → find largest and 2nd largest digit  
6. Rework (Q5) with dynamic array resizing for all digits  
7. Reverse a number using array  
8. BMI calculation for multiple persons (height, weight, BMI, category)  
9. Student marks in 3 subjects → percentage & grade (using arrays / 2D arrays)  
10. Frequency of digits in a number using array  

---

### ✅ Conventions
- One `.java` file per question, class name matches file name.  
- Use `Scanner` for inputs.  
- Maintain consistent structure across levels.  
- Array length property used in loops wherever applicable.  
